---
title: "Hands_On_EX08"
---

```{r}
pacman::p_load(sf, spdep, GWmodel, SpatialML, 
               tmap, rsample, Metrics, tidyverse)
```

```{r}
mdata <- read_rds("/Users/yuhu/Desktop/Geospatial Analytics and Applications/Hands-on-EX08/data/mdata.rds")
```

```{r}
set.seed(1234)
resale_split <- initial_split(mdata, 
                              prop = 6.5/10,)
train_data <- training(resale_split)
test_data <- testing(resale_split)
```

```{r}
write_rds(train_data, "/Users/yuhu/Desktop/Geospatial Analytics and Applications/Hands-on-EX08/data/train_data.rds")
write_rds(test_data, "/Users/yuhu/Desktop/Geospatial Analytics and Applications/Hands-on-EX08/data/test_data.rds")
```

```{r}
mdata_nogeo <- mdata %>%
  st_drop_geometry()
corrplot::corrplot(cor(mdata_nogeo[, 2:17]), 
                   diag = FALSE, 
                   order = "AOE",
                   tl.pos = "td", 
                   tl.cex = 0.5, 
                   method = "number", 
                   type = "upper")
```

```{r}
train_data <- read_rds("/Users/yuhu/Desktop/Geospatial Analytics and Applications/Hands-on-EX08/data/train_data.rds")
test_data <- read_rds("/Users/yuhu/Desktop/Geospatial Analytics and Applications/Hands-on-EX08/data/test_data.rds")
```

```{r}
price_mlr <- lm(resale_price ~ floor_area_sqm +
                  storey_order + remaining_lease_mths +
                  PROX_CBD + PROX_ELDERLYCARE + PROX_HAWKER +
                  PROX_MRT + PROX_PARK + PROX_MALL + 
                  PROX_SUPERMARKET + WITHIN_350M_KINDERGARTEN +
                  WITHIN_350M_CHILDCARE + WITHIN_350M_BUS +
                  WITHIN_1KM_PRISCH,
                data=train_data)
summary(price_mlr)
```

```{r}
write_rds(price_mlr, "/Users/yuhu/Desktop/Geospatial Analytics and Applications/Hands-on-EX08/data/price_mlr.rds" ) 
```

```{r}
train_data_sp <- as_Spatial(train_data)
train_data_sp
```

```{r}
bw_adaptive <- bw.gwr(resale_price ~ floor_area_sqm +
                  storey_order + remaining_lease_mths +
                  PROX_CBD + PROX_ELDERLYCARE + PROX_HAWKER +
                  PROX_MRT + PROX_PARK + PROX_MALL + 
                  PROX_SUPERMARKET + WITHIN_350M_KINDERGARTEN +
                  WITHIN_350M_CHILDCARE + WITHIN_350M_BUS +
                  WITHIN_1KM_PRISCH,
                  data=train_data_sp,
                  approach="CV",
                  kernel="gaussian",
                  adaptive=TRUE,
                  longlat=FALSE)
```

```{r}
write_rds(bw_adaptive, "/Users/yuhu/Desktop/Geospatial Analytics and Applications/Hands-on-EX08/data/bw_adaptive.rds")
```

```{r}
bw_adaptive <- read_rds("/Users/yuhu/Desktop/Geospatial Analytics and Applications/Hands-on-EX08/data/bw_adaptive.rds")
```

```{r}
gwr_adaptive <- gwr.basic(formula = resale_price ~
                            floor_area_sqm + storey_order +
                            remaining_lease_mths + PROX_CBD + 
                            PROX_ELDERLYCARE + PROX_HAWKER +
                            PROX_MRT + PROX_PARK + PROX_MALL + 
                            PROX_SUPERMARKET + WITHIN_350M_KINDERGARTEN +
                            WITHIN_350M_CHILDCARE + WITHIN_350M_BUS +
                            WITHIN_1KM_PRISCH,
                          data=train_data_sp,
                          bw=bw_adaptive, 
                          kernel = 'gaussian', 
                          adaptive=TRUE,
                          longlat = FALSE)
```

```{r}
write_rds(gwr_adaptive, "/Users/yuhu/Desktop/Geospatial Analytics and Applications/Hands-on-EX08/data/gwr_adaptive.rds")
```

```{r}
gwr_adaptive <- read_rds("/Users/yuhu/Desktop/Geospatial Analytics and Applications/Hands-on-EX08/data/gwr_adaptive.rds")
```

```{r}
gwr_adaptive
```

```{r}
test_data_sp <- test_data %>%
  as_Spatial()
test_data_sp
```

```{r}
gwr_bw_test_adaptive <- bw.gwr(resale_price ~ floor_area_sqm +
                  storey_order + remaining_lease_mths +
                  PROX_CBD + PROX_ELDERLYCARE + PROX_HAWKER +
                  PROX_MRT + PROX_PARK + PROX_MALL + 
                  PROX_SUPERMARKET + WITHIN_350M_KINDERGARTEN +
                  WITHIN_350M_CHILDCARE + WITHIN_350M_BUS +
                  WITHIN_1KM_PRISCH,
                  data=test_data_sp,
                  approach="CV",
                  kernel="gaussian",
                  adaptive=TRUE,
                  longlat=FALSE)
```

```         
```

```{r}
coords <- st_coordinates(mdata)
coords_train <- st_coordinates(train_data)
coords_test <- st_coordinates(test_data)
```

```{r}
coords_train <- write_rds(coords_train, "/Users/yuhu/Desktop/Geospatial Analytics and Applications/Hands-on-EX08/data/coords_train.rds" )
coords_test <- write_rds(coords_test, "/Users/yuhu/Desktop/Geospatial Analytics and Applications/Hands-on-EX08/data/coords_test.rds" )
```

```{r}
train_data <- train_data %>% 
  st_drop_geometry()
```

```{r}
set.seed(1234)
rf <- ranger(resale_price ~ floor_area_sqm + storey_order + 
               remaining_lease_mths + PROX_CBD + PROX_ELDERLYCARE + 
               PROX_HAWKER + PROX_MRT + PROX_PARK + PROX_MALL + 
               PROX_SUPERMARKET + WITHIN_350M_KINDERGARTEN +
               WITHIN_350M_CHILDCARE + WITHIN_350M_BUS + 
               WITHIN_1KM_PRISCH,
             data=train_data)
rf
```

```{r}
write_rds(rf, "/Users/yuhu/Desktop/Geospatial Analytics and Applications/Hands-on-EX08/data/rf.rds")
```

```{r}
rf <- read_rds("/Users/yuhu/Desktop/Geospatial Analytics and Applications/Hands-on-EX08/data/rf.rds")
rf
```

```{r}
set.seed(1234)
gwRF_adaptive <- grf(formula = resale_price ~ floor_area_sqm + storey_order +
                       remaining_lease_mths + PROX_CBD + PROX_ELDERLYCARE +
                       PROX_HAWKER + PROX_MRT + PROX_PARK + PROX_MALL +
                       PROX_SUPERMARKET + WITHIN_350M_KINDERGARTEN +
                       WITHIN_350M_CHILDCARE + WITHIN_350M_BUS +
                       WITHIN_1KM_PRISCH,
                     dframe=train_data, 
                     bw=55,
                     kernel="adaptive",
                     coords=coords_train)
```

```{r}
write_rds(gwRF_adaptive, "/Users/yuhu/Desktop/Geospatial Analytics and Applications/Hands-on-EX08/data/gwRF_adaptive.rds")
```

```{r}
gwRF_adaptive <- read_rds("/Users/yuhu/Desktop/Geospatial Analytics and Applications/Hands-on-EX08/data/gwRF_adaptive.rds")
```

```{r}
test_data <- cbind(test_data, coords_test) %>%
  st_drop_geometry()
```

```{r}
gwRF_pred <- predict.grf(gwRF_adaptive, 
                           test_data, 
                           x.var.name="X",
                           y.var.name="Y", 
                           local.w=1,
                           global.w=0)
```

```{r}
GRF_pred <- write_rds(gwRF_pred, "/Users/yuhu/Desktop/Geospatial Analytics and Applications/Hands-on-EX08/data/GRF_pred.rds")
```

```{r}
GRF_pred <- read_rds("/Users/yuhu/Desktop/Geospatial Analytics and Applications/Hands-on-EX08/data/GRF_pred.rds")
GRF_pred_df <- as.data.frame(GRF_pred)
```

```{r}
test_data_p <- cbind(test_data, GRF_pred_df)
```

```{r}
write_rds(test_data_p, "/Users/yuhu/Desktop/Geospatial Analytics and Applications/Hands-on-EX08/data/test_data_p.rds")
```

```{r}
rmse(test_data_p$resale_price, 
     test_data_p$GRF_pred)
```

```{r}
ggplot(data = test_data_p,
       aes(x = GRF_pred,
           y = resale_price)) +
  geom_point()
```
